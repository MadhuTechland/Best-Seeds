// Intentionally empty.
//
// Xcode generates a regular (home-screen) Widget stub in every Widget
// Extension template, but we only ship the Live Activity here. Keeping
// the file (so Xcode's project references stay intact) but emptying its
// body keeps the target lean. The Bundle does not register this widget.
import Foundation
